Sticky headers are used to fix the date-headers at the top of the calendar to the viewport while scrolling.

stickyHeaderDates: true
->fix the date-headers at the top of the calendar to the viewport while scrolling